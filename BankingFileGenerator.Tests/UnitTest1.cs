using Microsoft.VisualStudio.TestTools.UnitTesting;
//using BankingFileGenerator.Lib.Generators;

namespace BankingFileGenerator.Tests
{
    [TestClass]
    public class DemoTests
    {
        [TestMethod]
        public void ShouldGenerateFile()
        {
           // var gen = new Bai2Generator();
          //  var content = gen.GenerateFile();
         //   Assert.IsNotNull(content);
         //   Assert.IsTrue(content.Contains("BAI2"));
        }
    }
}
// This code defines a unit test for the Bai2Generator class in the BankingFileGenerator application.